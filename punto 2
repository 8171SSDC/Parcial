##Se solicita al usuario que ingrese un número entero.
n = int(input("Ingresa un número entero positivo: "))
## Se verifica que el número es menor que 1, si es así se imprime el mensaje de que tiene que ser un numero positivo
if n < 1:
    print("Por favor, ingresa un número entero positivo.")
else:
    while n != 1:
        print(n, end=' ')
        n = n // 2 if n % 2 == 0 else 3 * n + 1
    print(1)
##Si el número es válido, se ejecuta el bucle que imprime la secuencia hasta llegar a 1